# Web Framework
Flask~=3.0
flask-cors~=4.0

# LangChain ecosystem — pin these tightly, they move fast
langchain~=0.3
langchain-community~=0.3
langchain-groq~=0.2
langchain-huggingface~=0.1
langchain-chroma~=0.1

# Vector DB
chromadb~=0.5

# Embeddings (HuggingFace, runs fully local — no API key)
sentence-transformers~=3.0

# Document loaders
pypdf~=4.0           # PDF parsing (more maintained than PyPDF2)
python-docx~=1.1     # DOCX parsing
beautifulsoup4~=4.12 # Web scraping (used by WebBaseLoader)
lxml~=5.2            # Faster HTML parser for bs4

# HTTP + async
requests~=2.32
httpx~=0.27

# Config & utilities
python-dotenv~=1.0
pydantic~=2.0        # Data validation for request payloads
pydantic-settings~=2.0

# Logging
python-json-logger~=2.0   # Structured JSON logs (parseable by Datadog, etc.)